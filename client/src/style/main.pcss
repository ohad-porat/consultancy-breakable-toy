@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  html {
    @apply font-mono font-normal bg-zinc-100;
    font-size: 18px;
    line-height: 24px;
  }
}

.page-body {
  @apply mx-14;
}

/* .something {
  @apply bg-red-500;
  & .something-else {
    @apply bg-blue-400;
  }
} */
