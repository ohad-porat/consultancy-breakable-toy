@tailwind base;
@tailwind components;
@tailwind utilities;

@layer base {
  html {
    @apply font-mono font-normal;
    font-size: 18px;
    line-height: 24px;
  }
}

.something {
  @apply bg-red-500;
  & .something-else {
    @apply bg-blue-400;
  }
}
